{
  "name": "homeguard",
  "version": "0.1.0",
  "private": true,
  "description": "Protect against accidentally opening the home directory in VS Code.",
  "bin": {
    "homeguard-code": "dist/src/cli.js",
    "workspace-guard-scan": "dist/src/githubScanCli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
